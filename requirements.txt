# Core ML / Vision
ultralytics>=8.2.0          # RT-DETRv2-X (DINO-style decoder) + YOLOv8 detection
torch>=2.0.0                # PyTorch deep learning framework
torchvision>=0.15.0         # Torchvision utilities (used by ultralytics)

# Computer Vision
opencv-python>=4.8.0        # Frame I/O, Kalman filter, drawing utilities

# Numerical
numpy>=1.24.0               # Array operations throughout the pipeline
scipy>=1.10.0               # linear_sum_assignment for Hungarian matching

# Progress / CLI
tqdm>=4.65.0                # Progress bar for frame-level loop

# Data export
pandas>=2.0.0               # Optional: post-processing / analysis of output CSV

# SAHI & Supervision additions
sahi>=0.11.0                # DINO/ultralytics adapter + improved tile fusion
supervision>=0.20.0

# Vehicle Re-Identification
torchreid>=0.2.5            # OSNet-x0.25 for vehicle-specific appearance embeddings

# ONNX (optional — for DINO model export/optimisation)
onnx>=1.14.0                # ONNX graph optimisation for deployment
